# Wave B2 - Sources You Can Trust (2026-09-13)

Branch `build/wave-b2`, owner-preview-gated (reshapes the reading screen). Not merged, not pushed, main untouched.

## What shipped, against the six items

1. **The six real commentaries reach Home.** `src/components/study/CommentaryCard.tsx` now sources Home's Commentary block from the study library (one tab per commentary that actually has text for the open chapter, cached per chapter in memory via `useMemo`/module cache, loading state that never blanks the passage); the curated 20-chapter set survives only as the first tab when it exists for the chapter (`CURATED_ID`, line ~29-34). Mounted into `HomeScreen.tsx` in place of the old `allCommentaries`/AI-as-tab strip.
2. **A provenance line under every sourced entry.** `src/utils/provenance.ts` (`formatProvenance`, `provenanceForSource`) formats name + edition + licence, name-alone when metadata is missing; fed by `src/utils/useStudySources.ts` (session-memoised `study_sources` map). Used under each `CommentaryCard` entry and under `src/components/GreekHebrewPopup.tsx`'s result.
3. **The AI paragraph in its own labelled block.** `CommentaryCard.tsx` renders the AI paragraph below the sourced tabs, not as a peer tab, headed by an i18n label and its own provenance line naming the model; the empty-state copy path (no commentary at all) falls back to the deeper_study.md ~295 line, all four languages in `src/utils/i18n.ts`.
4. **The AI commentary prompt stops asking for a cross-reference.** The cross-reference ask removed from `HomeScreen.tsx`'s default commentary prompt (near former ~1462-1466) and from the pastor quick-prompts in `src/components/BibleAI.tsx`; `src/utils/pastorPrompts.ts` was already verified clean and untouched per the brief's out-of-scope note.
5. **Cross-references from the study layer.** `src/components/study/CrossRefsCard.tsx` adds a Cross-references entry beside Commentary/Compare for deeper_study and pastor_leader, sourced from the study layer with a provenance line, each reference tappable to open that passage in the existing passage viewer.
6. **The pastor AI is a study assistant, not a ghostwriter.** Framing line added under the four pastor prompts (`src/components/BibleAI.tsx`, `src/sections/BibleAIPromptSection.tsx`, `src/utils/persona-config.ts`); Home teaser reframed to name sources rather than output. "Where this comes from" opens `src/components/StudySourcesCard.tsx` as a sub-view via `useSubView` alone, mounted in `HomeScreen.tsx` with a live `open=` prop (`src/components/study/StudySourcesSheet.tsx`), confirmed by `src/screens/home-wave-b2.test.ts` (not a conditional render / no focus trap).

Tests added: `src/utils/provenance.test.ts` (formatter unit tests), `src/screens/home-wave-b2.test.ts` (source-contract: AI block outside and below the tab strip, no cross-reference ask in the commentary prompt, StudySourcesSheet mounted via useSubView with a live open prop), `src/components/BibleAI.pastor.test.tsx` (pastor mode renders the framing line). Also added `CommentaryCard.test.tsx`, `CrossRefsCard.test.tsx`, `StudySourcesSheet.test.tsx`.

## Two data facts to check on the preview

- **`study_commentary`**: whether it actually holds text for the six commentaries (Matthew Henry, Jamieson Fausset & Brown, John Gill, Adam Clarke, John Calvin, Keil & Delitzsch) for the chapters you open. If a chapter is empty for a given commentary, that tab simply does not render for it - by design, not a bug.
- **`study_crossrefs`**: whether it is loaded at all. If empty, the Cross-references entry shows its honest empty state.

If either table is empty in the live project, the fix is a data load, not a code change:

```
node scripts/load-study-data.mjs commentary crossrefs
```

## The unapplied migration

`supabase/migrations/20260913_study_sources_edition.sql` adds `study_sources.edition` and is **unapplied by default**. The sources endpoint (`netlify/functions/study.js`) tolerates its absence - the edition half of every provenance line (source name + edition + licence) stays blank until the column exists and a data load fills it in. This is expected on the preview until the migration is applied.

## One judgement call to confirm

The curated 20-chapter set (the set that used to be labelled "Matthew Henry" / "Thayer's" even though it was Daily Word's own curated prose, not those named authors) now renders under the honest label **"Daily Word editors"** wherever it appears as the first commentary tab (`CommentaryCard.tsx` ~33: "the reader never sees which source, only the honest 'Daily Word editors' label"). Confirm this is the label you want, or supply the label you prefer.
