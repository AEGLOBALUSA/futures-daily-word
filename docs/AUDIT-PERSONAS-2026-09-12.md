# Daily Word persona audit, 12 Sep 2026

Question: is the live app (main e81b0dc1, futuresdailyword.com) doing what `docs/PERSONA-RESEARCH-2026-09-10.md` says each persona needs? Method: one opus auditor per live path read the research section and the live code and classified every research need as delivered, partial, missing or contradicted, plus up to six defects where the path breaks its own door promise; one sonnet verifier per claim tried to refute it from the code. Only claims that survived are listed; refuted claims are listed with the refutation. Workflow `wf_7317a4b3-4d6`, 113 agents. The parent (Fable) drove the five doors in a browser the same evening; those observations are at the end.

Build check on the same commit: `npm run build` PASS, `npx vitest run` PASS (53 files, 393 tests).


## New to faith (research persona 1)

**Door:** Title: "I'm new to faith, or coming back" — Promise: "A 40-day journey. One short day at a time." — CTA: "Start Day 1" (src/utils/i18n.ts:923-925, keys wired at src/utils/choosePath.ts:31, rendered one card per path at src/components/ChoosePathSheet.tsx:205-229)

**What opens:** On a cold device nothing of Home shows first: App.tsx:235-236 renders Day1Landing instead — wordmark, language switch, "Day 1 of 40 · New & Returning to Faith", "Grace Changes Everything", one "Read" button, and a quiet "Not new to faith? Choose your path" link; tapping Read expands KJV Ephesians 2:8-9, three pastoral paragraphs, two Reflect & Respond questions and "Mark as read" on the same screen (src/components/Day1Landing.tsx:91-151). After that (or for anyone who picked this door), Home renders sectionOrder ['greeting','hero_audio','scripture','faith_pathway'] (src/utils/persona-config.ts:284-289) as: header with NO Bible AI pill and NO campus chip (HomeScreen.tsx:1895, 1979-1996), date + "Daily Word", and on the right the path swatch, language and theme toggles but NO streak counter (HomeScreen.tsx:2006); centred greeting "Welcome, {first}. We're glad you're here." with no search icon (persona-config.ts:234-235, features.searchEnabled false at 300); a one-time path-arrival strip "You're on the 40-day journey. Today's day is the card below." (HomeScreen.tsx:2097-2104, i18n.ts:916); then the black Sermon Notes banner (HomeScreen.tsx:2106); then, for the first three days only, "Each day: one scripture, one short teaching, one step. About five minutes." (HomeScreen.tsx:2110-2118, i18n.ts:402); then the sage journey hero — photo carousel, "Day N of 40 · New & Returning to Faith", the day title, a progress bar and one sage Read button that opens the full-screen Day N surface (HomeScreen.tsx:2155-2215; NewBelieverLessonCard mounted at 4542-4559). The 'scripture' section renders nothing (HomeScreen.tsx:3434-3442), and For You, promo ads and campus count are all off (HomeScreen.tsx:4163, 4583; persona-config.ts:299).

**Verdict:** The path delivers the research's first need almost perfectly — a cold device lands on one short Day 1 with one button and nothing asked — and then breaks its own promise from Day 2 onward: the 40-day journey's 1–3 assigned verses are served as whole chapters of Romans and Ephesians in doctrine-first order, a sermon-notes banner leads the page instead of the reading, there is no glossary and no relational move, and the day-count this path hides everywhere still arrives as a flame overlay counting app opens.

Claims verified: 18, held 18, refuted 0.


### Contradicted (the live code does the opposite)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| A short unit, not a chapter — one thought plus a few verses | The journey data assigns 1–3 verses a day (day 1 Ephesians 2:8-9, day 2 2 Corinthians 5:17), but the app deliberately throws the range away and loads the whole chapter, then renders every parsed verse of it under a label that still prints the short reference. Day 2 hands a new believer all 21 verses of 2 Corinthians 5 titled '2 Corinthians 5:17'. | src/screens/HomeScreen.tsx:853-857, src/screens/HomeScreen.tsx:1306-1309, src/components/NewBelieverLessonCard.tsx:94, src/components/NewBelieverLessonCard.tsx:179, src/components/NewBelieverLessonCard.tsx:191-195, src/components/ScripturePassage.tsx:40, src/components/ScripturePassage.tsx:117 |
| Narrative first (Gospel scenes), doctrine later — they quit early, in the hard opening material | The live 40-day order is doctrine-first and epistle-heavy: days 1–9 are Ephesians 2, 2 Corinthians 5, John 19:30, Romans 5, Romans 5, 1 John 4, Romans 8, Romans 3, 1 John 1. Only two of the first fourteen readings are Gospel text and neither is a scene. The order is fixed in the content file; no reorder or narrative-first logic exists in code. | books/faith-pathway.json days 1-14, src/screens/HomeScreen.tsx:850-857, src/components/NewBelieverLessonCard.tsx:77 |

### Defects (the path breaks its own promise)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| The door promises 'One short day at a time' and the app serves a whole chapter: the day's assigned verse range is discarded when the passage is loaded and the full chapter is rendered under the short reference as its label. | severity: high | src/screens/HomeScreen.tsx:855-857 ('Use full chapter as the reading'), src/screens/HomeScreen.tsx:1306-1309, src/components/NewBelieverLessonCard.tsx:94 + :179 + :191-195, src/components/ScripturePassage.tsx:117, src/utils/i18n.ts:924 |
| The Sermon Notes banner renders unconditionally as the first card on Home for every path including this one — one line below an arrival strip that says 'Today's day is the card below'. The comment above it claims I'm-New keeps it only in the Sunday window, but isSundayWindow is never imported into HomeScreen and sermonNotesRow gates only the pastor destination. | severity: high | src/screens/HomeScreen.tsx:2106, src/screens/HomeScreen.tsx:1774-1786, src/utils/sunday.ts:9-21 (never imported in HomeScreen), src/utils/i18n.ts:916 |
| A guilt/reward day-count reaches this path through the back door: recordStreakToday() runs in a bare mount effect, so the streak counts app opens rather than readings, and the un-gated milestone overlay then shows a 56px flame with '{x} Days!' — the very count the header and the greeting deliberately hide on this persona. | severity: high | src/screens/HomeScreen.tsx:785-793, src/screens/HomeScreen.tsx:4437-4468 (🔥 at :4457), src/screens/HomeScreen.tsx:2006, src/utils/persona-config.ts:111-120, src/utils/streak.ts:13 |
| The only scripture a brand-new believer ever reads on Day 1 is hardcoded KJV ('For by grace are ye saved through faith... lest any man should boast'), ignoring the reader's translation, against the path's 'plain language' need. | severity: medium | src/data/day1-landing.ts:37-38 (DAY1_VERSE_TEXT) and :59-63 (day1Copy verseText assignment); src/components/Day1Landing.tsx:133-136 |
| The daily push reminder for this path never names their journey day: it sends one global day-of-year chapter from a 40-entry rotation to every subscriber, so a reader on Day 2 (2 Corinthians 5:17) is nudged toward, say, 'Psalms 23'. Templates and verse snippets exist only in en and id, so Spanish and Portuguese readers get English pushes. | severity: medium | netlify/functions/push-send.js:24-30, netlify/functions/push-send.js:150-155, netlify/functions/push-send.js:204-215, netlify/functions/push-send.js:120-139, src/screens/HomeScreen.tsx:483 |
| Tapping a verse to ask what it means is not a read-only action: the tap writes a persistent highlight and an auto-created Notes entry before the 'What this means' button is even visible, so a beginner exploring the text accumulates highlights and notes they never asked for. | severity: low | src/contexts/ScriptureSelectionContext.tsx toggleHighlight (~lines 158-182), called from src/components/ScripturePassage.tsx:44-48 |

### Missing

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Two doors inside one card, 'New to this' and 'Coming back', same journey, different first line | There is one door, one headline covering both, one CTA and one destination. The chooser renders exactly one card per persona from PATHS, and no code branches a first line on new-vs-returning; the only other entry (the asked-once screen after the first read) also asks a single combined question. | src/utils/choosePath.ts:26-33, src/components/ChoosePathSheet.tsx:205-229, src/utils/i18n.ts:923-925, src/utils/i18n.ts:948 |
| One relational move by day 10 — 'someone at Futures would love to read this with you', as an intake action | Nothing on this path invites a human connection. Grep for 'read this with you'/'read_with'/intake actions finds nothing; the campus chip, campus count, promo ads and For You are all suppressed for this persona, and the only outward link on the whole screen is the Sermon Notes chooser. Day 24's lesson text mentions Connect Groups as content, with no action attached. | src/utils/persona-config.ts:292-306, src/screens/HomeScreen.tsx:1979-1996, src/screens/HomeScreen.tsx:4163, src/screens/HomeScreen.tsx:4583, books/faith-pathway.json day24 |

### Partial

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| A guide through complexity (the 'Bible Curious' welcome one) | One explanatory line exists — 'Each day: one scripture, one short teaching, one step. About five minutes.' — but only while completedDays < 3, and the AI that would answer a beginner's question is removed from this path's Home header, so the only guide after day 3 is a text selection inside the reading. | src/screens/HomeScreen.tsx:2110-2118, src/utils/i18n.ts:402, src/screens/HomeScreen.tsx:1895, src/utils/persona-config.ts:320-322 |
| Literacy scaffolding — a one-line 'what this word means' under any hard word | No glossary exists anywhere in the repo (grep for 'glossar' over src, books, data, public returns nothing). What exists is verse-level and AI-mediated: selecting/tapping a verse raises the I'm-New toolbar with 'What this means', which opens Bible AI pre-filled with 'What does this verse mean? Explain it in simple, everyday language.' Word-level tools (greekHebrew, wordStudies) are hidden by config, and the verse tap also writes a persistent highlight plus a Notes entry. | src/components/HighlightToolbar.tsx:146-160, src/screens/HomeScreen.tsx:4565-4578, src/utils/i18n.ts:322, src/utils/persona-config.ts:292-303, src/components/ScripturePassage.tsx:44-48 |
| Ordinary, no-explanation re-entry for the returning | Nothing is asked before the reading and the door names both people in one line ('I'm new to faith, or coming back'), but there is no returning-specific line anywhere after the door: the greeting is the same 'Welcome, {first}. We're glad you're here.' on day 1 and day 39, and the one line that acknowledges a returning reader ('Day 1 — your longest run is {best} days') is explicitly excluded for this persona. | src/utils/i18n.ts:923, src/utils/persona-config.ts:234-235, src/utils/persona-config.ts:111-120, src/utils/i18n.ts:425 |
| Day 1: 3–7 verses, plain language, one thought, already open | The cold-start Day 1 is the right size — two verses (Ephesians 2:8-9), three plain paragraphs, one thought — but it is NOT already open (the hero stays closed behind the Read button, readingOpen=false until tapped), and the verse text is hardcoded KJV rather than plain modern English. | src/components/Day1Landing.tsx:42, src/components/Day1Landing.tsx:98-122, src/data/day1-landing.ts:41-45, src/data/day1-landing.ts:21-27 |

### Delivered

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| A starting point — a zero-decision first move | A cold device is written to new_to_faith, enrolled at Day 1 of the 40-day journey with chapters_per_day=1, and shown a one-button Day 1 screen; no picker, no email, no plan choice. A real earlier choice is never overwritten. | src/utils/coldStart.ts:52-110, src/utils/coldStart.ts:186-196, src/App.tsx:68, src/App.tsx:235-239, src/components/Day1Landing.tsx:98-108 |
| Complete on the same screen (research notes this is already true) | Both Day 1 surfaces finish where they read: Day1Landing carries verse, teaching, questions and 'Mark as read' in one scroll, and the full-screen Day N carries verses, lesson, questions and 'Mark Complete' in one scroll with the completion note held on screen. | src/components/Day1Landing.tsx:123-151, src/components/NewBelieverLessonCard.tsx:163-290 |
| Progress ring visible from day one; credit-free seed stays credit-free | A sage progress bar (completedDays/total) sits on the hero and again on the Day N card from day one, and this path is explicitly exempted from the 'arrival is the reading' auto-seed, so nothing is credited until the reader taps Mark Complete; completion routes through savePathwayProgress, which only then records the streak. | src/screens/HomeScreen.tsx:2184-2186, src/components/NewBelieverLessonCard.tsx:144-153, src/screens/HomeScreen.tsx:1450-1457, src/screens/HomeScreen.tsx:541-550 |
| The two journey questions per day stay under the teaching (ruled 2 Sep) | Each pathway day carries exactly two questions, and PathwayQuestions renders them directly under the lesson text and above Mark Complete on the Day N surface (and on the Day 1 landing), answers saved per day in localStorage with no account required. | books/faith-pathway.json days[0].questions (not a "day1" key — data is a days array indexed by position) |

## Congregation, 3-minute devotional (persona 2)

**Door:** Door title: "I'm part of Futures Church" (src/utils/i18n.ts:926, key path_member_head, wired at src/utils/choosePath.ts:32). Promise line: "Today's reading with Ashley & Jane, already open." (src/utils/i18n.ts:927, path_member_promise). CTA: "Open today's reading" (src/utils/i18n.ts:928). Arrival strip after picking: "You're on today's reading with Ashley & Jane. It's already open below." (src/utils/i18n.ts:914, path_arrival_member).

**What opens:** sectionOrder for congregation is ['greeting','hero_audio','scripture','ai_prompt','campus_count'] (src/utils/persona-config.ts:336-342), but Home also renders several ungated blocks. Top to bottom as the live code renders it: (1) header — long date, "Daily Word", campus chip, then the streak counter "N days" plus an encouragement label, the path swatch, language switch, theme toggle (src/screens/HomeScreen.tsx:1873-2050, streak block 2006-2045); (2) greeting "Good {tod}, {Name}. Day N — keep going." with a labelled Search button (2054-2092; greeting text src/utils/persona-config.ts:237-241); (3) one-time path arrival strip (2095-2103); (4) the Sermon Notes banner, unconditional, above everything to read (2106, built 1786-1831); (5) the hero card — photo carousel that is itself the play button, top tag "Daily Word with Ashley & Jane — Day N", the chapter name in 27px serif, "ESV · HUMAN READER", chapter pills/slider when the day has more than one chapter, then a Read/Hide + Restart + translation-pill deck, and under it the white reading panel with the FULL CHAPTER already expanded on arrival, an InlineReflection journal box and a "Mark as read" button (2120-2760; arrival auto-expand 1449-1456; reading panel 2640-2725); (6) the dark date-navigation strip repeating "TODAY'S READING" + "Day N of Daily Word with Ashley & Jane · <chapter>" (2768-2823); (7) the Bible AI prompt card, until first AI use (2918 → src/sections/BibleAIPromptSection.tsx:24-27); (8) the "Go Deeper?" upgrade prompt (2974-2984); (9) the Commentary card, collapsed, whenever curated commentary exists for the day's chapter (3355-3364; features.commentary='collapsed', persona-config.ts:344); (10) a second full card headed "TODAY'S CHAPTERS" that prints the same plan chapter again with its own Listen button and its own Read button (3434, label 3454, plan-passage cards 3535-3620); (11) the daily quote (3918-3950); (12) the campus card — a campus *picker*, no count (3973-4057); (13) the active-plans strip (4063+); (14) the "For You" behavioural card once 5+ events exist (4163-4227); (15) PromoAds book/college mosaic (4582).

**Verdict:** Today this path delivers the frictionless start and the reading-with-audio card the research asks for, but it fails the two things that define the persona: the unit is a whole chapter rather than three minutes, and Ashley & Jane's one thought — the name on the door — is dead code behind a section key no persona holds; the habit layer is also weaker than it looks, since the day-count it advertises counts app opens rather than readings, and nothing seasonal, comfort-framed or behaviour-timed exists at all.

Claims verified: 24, held 23, refuted 1.


### Contradicted (the live code does the opposite)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Momentum cues: streak, day-count — a count that means something | The day-count counts OPENS, not readings. recordStreakToday() fires from a bare mount effect on HomeScreen with no dependency on reading anything, so merely launching the app advances 'Day N — keep going', the header 'N days' counter and the 7/14/30-day 🔥 milestone modal. The app's own comment at HomeScreen.tsx:1843-1845 admits the streak 'records on HomeScreen mount', which is why the push ask had to be re-keyed off it. | The HomeScreen mount comment is at HomeScreen.tsx:784-793, not exactly 1843-1845 (that block is an unrelated DoneCelebration/push-event handler); otherwise all cited evidence checks out. |
| A reading sized to three minutes | The live unit is the opposite of small. The auto-started plan is 100 days of whole chapters and the arrival effect expands the entire chapter immediately, so the persona defined by three minutes lands on the longest available unit with no way to shrink it — and the same chapter is then printed a second time lower down the page. | src/data/plans.ts:69-82, src/data/ashley-jane-plan.ts:2602-2670, src/screens/HomeScreen.tsx:1449-1456, 3434-3620 |
| "Nothing sits above today's reading" (this path's own arrival promise, "already open below") | The Sermon Notes banner renders unconditionally between the greeting and the hero on every path, contradicting the comment sitting directly above it in the same file, which states it renders BELOW the reading for the four returning personas because 'nothing sits above today's reading'. It is also not gated on features.sermonNotes or on any Sunday window. | src/screens/HomeScreen.tsx:1775-1785, 1786-1831, 2105-2106 |

### Defects (the path breaks its own promise)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Ashley & Jane's devotional thought — the thing the door promises by name — can never render on this path. todaysDevotion is computed from the plan's devotionals, but its only consumer is gated on sectionOrder.includes('devotion_scripture'), and no persona in PERSONA_CONFIGS holds that key. A Church Member gets the plan title 'Daily Word with Ashley & Jane' and a raw chapter; the 100 authored devotionals (their voice, their stories) are unreachable UI. | severity: high | src/screens/HomeScreen.tsx:929-937 (todaysDevotion), 3106-3110 (dead gate), src/utils/persona-config.ts:336-342 (no devotion_scripture key), src/data/ashley-jane-plan.ts:13-33 |
| The day-count the greeting shows is a count of app opens. recordStreakToday() runs in an empty-dependency mount effect, so 'Day N — keep going', the header counter and the 🔥 '{N} Days!' milestone modal all advance on launch alone, with nothing read. For the one persona whose research explicitly warns that streak mechanics stop working when they stop being honest, the headline number is not evidence of reading. | severity: high | src/screens/HomeScreen.tsx:785-795, 2006-2045, 4437-4470; src/utils/streak.ts:41-90; the app's own note at src/App.tsx:266-270 |
| handleRead silently credits a plan day on a mere expand, and can fire the plan-finish celebration from it. Tapping Read (after a Hide, or on the duplicate TODAY'S CHAPTERS card) calls markPlanDayComplete(passage) and, if that was the last day, setPlanFinish — while dw_reading_done and the streak are untouched. So the plan advances and the finish-line 'Plan complete 🎉' can appear without the deliberate 'Mark as read' the code comment three lines up says completion requires ('focus-group: completion needs intent'). | severity: medium | src/screens/HomeScreen.tsx:733-757 (credit at 755), 712-731 (handleMarkRead and its comment), 683-710 (markPlanDayComplete) |
| The same chapter is printed twice on one screen. The hero card shows today's plan chapter with a Listen control, a Read/Hide toggle and the expanded text; a second full card headed 'TODAY'S CHAPTERS' then renders the identical todaysPlanPassages entry with its own full-width Listen button and its own Read button. The de-duplication that exists (slotsOnlyMode) only triggers for personas that ALSO have 'plan_scripture' — which congregation does not — so this path is the one that gets the duplicate. | severity: medium | src/screens/HomeScreen.tsx:1291-1297 (hero refs), 3446-3450 (slotsOnlyMode requires plan_scripture), 3454 (header), 3535-3620 (duplicate card), src/utils/persona-config.ts:336-342 |
| The Sermon Notes banner is the first tappable thing on a screen whose promise is 'today's reading, already open'. It renders at HomeScreen.tsx:2106 — above the hero — unconditionally, with no features.sermonNotes gate and no Sunday window, directly contradicting the comment at 1775-1785 that says it sits BELOW the reading for the four returning personas precisely so nothing sits above today's reading. | severity: medium | src/screens/HomeScreen.tsx:1775-1785, 2105-2106 |
| Spanish and Portuguese Church Members get English reminders. The door, promise, arrival strip and greeting are all translated to es/pt (i18n.ts:926-928, 914; persona-config.ts:122-157), but push TEMPLATES and VERSE_SNIPPETS exist only in en and id, and getTemplate/getVerseSnippet fall back to English for any other lang — so the one retention mechanism this persona has arrives in the wrong language for two of the four shipped locales. | severity: medium | netlify/functions/push-send.js:121-138, 142-149, src/utils/push.ts:130-135, src/utils/i18n.ts:926-928 |

### Missing

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Comfort-framed copy (Isaiah 41:10 four years running); comfort language belongs on every path | Nothing on this path is comfort-framed. The greeting is pure momentum ('Day N — keep going', 'N days strong!'), the AI card says 'Go deeper with Bible AI', and the comfort blocks (ComfortVerseBannerSection, ComfortSection) are gated on comfort-only sectionOrder keys so they never mount here. The only route to comfort is switching your whole path in the chooser sheet. | src/utils/persona-config.ts:237-241, src/screens/HomeScreen.tsx:2887, 2892, src/sections/BibleAIPromptSection.tsx:24-27, src/utils/choosePath.ts:32 |
| "Read more" opens the full chapter for the days they have time | There is no progressive disclosure to build on: the full chapter is the only unit and it is expanded by default on arrival. No verse-range view, no excerpt, no 'read more' control exists (the hero's only text control is Read/Hide, which toggles the whole chapter). | src/screens/HomeScreen.tsx:1449-1456, 2521-2545, 2640-2725, src/data/ashley-jane-plan.ts:2602-2670 |
| Seasonal 21-day and 40-day church-wide reads with a live count of people reading today — "this is where the spikes come from" | Nothing. Grepping the whole src tree for lent / seasonal / church-wide / 'reading today' returns only the pastor analytics card and two dead components; the plan catalogue has no dated or seasonal entry, and no church-wide event surface or live participant count exists on any non-pastor path. | src/data/plans.ts:65-82, src/screens/HomeScreen.tsx:4235-4260, src/sections/CampusCountSection.tsx:1-35 (never imported — only src/sections/index.ts:8) |
| Time-boxed shared events as a momentum cue | Absent for the same reason as the seasonal reads. The 'campus_count' key in this persona's sectionOrder resolves only to a campus PICKER card (pf.campusCount !== 'hidden'), not to any shared or time-boxed surface. | src/utils/persona-config.ts:341, src/screens/HomeScreen.tsx:3973-4057 |

### Partial

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Time and habit scaffolding (busyness is the #1 reason reading declines) | The habit half exists — streak, day-count, milestones, an evidence-timed reminder opt-in. The time half does not: the unit of work is a whole Bible chapter (Hebrews 11, 1 Samuel 17, Matthew 13, Psalm 119 all appear in the plan), opened in full, with no shorter option and no time estimate anywhere. | src/data/ashley-jane-plan.ts:2602-2670, src/screens/HomeScreen.tsx:1449-1456, 2006-2045 |
| A reading sized to three minutes, verse plus Ashley & Jane's one thought | Partly there and partly unreachable. The reading is a full chapter, not a 3-minute unit. Ashley & Jane's written thought exists in the data (100 devotionals, 300+ words each, wired into todaysDevotion) but the ONLY block that renders it is gated on sectionOrder.includes('devotion_scripture') — a key no persona holds — so on the live congregation Home their names appear on the plan label and nothing else. | src/screens/HomeScreen.tsx:929-937, 3106-3110, src/utils/persona-config.ts:336-342, src/data/ashley-jane-plan.ts:13-33 |
| Reminder: infer the time they actually open and nudge then | The delivery machinery is right (per-timezone hour matching, once a day, catch-up window) but the hour is USER-SET, defaulting to 07:00 — nothing infers it. behavior.ts stores a ts on every event so the data to infer it exists, but no code reads an hour out of it; getPushHour() just reads dw_push_hour or returns 7. | src/utils/push.ts:185-188, src/components/PushOptIn.tsx:8-13, 44-58, netlify/functions/push-subscribe.js:51, src/utils/behavior.ts:47-59 |
| Missed days: streak repair, one tap, no shame | The no-shame half is delivered: a single missed day is silently absorbed by a one-per-week 'freeze' that keeps the count climbing, and a genuine reset greets with 'Day 1 — your longest run is {best} days. It starts the same way.' The repair half is missing: the freeze is automatic and invisible (no UI anywhere — grep for 'freeze' finds only streak.ts), and a two-day gap resets to 1 with no tap offered to repair it. | src/utils/streak.ts:63-79, src/utils/persona-config.ts:111-120, src/utils/i18n.ts:425, src/components/DoneCelebration.tsx:27-35 |

### Delivered

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Low-friction start — a zero-decision first move | Picking the Church Member door auto-starts the 'Daily Word with Ashley & Jane' plan once, with no picker, no modal and no form; the hero then lands on that plan's day. It is once-only (dw_aj_autostarted) so a later quit is respected, and it never overwrites an existing plan or reading slots. | src/screens/HomeScreen.tsx:977-993, src/utils/persona-config.ts:16, src/data/plans.ts:69-82 |
| Day 0: one starter reading, no picker | True as the research says. The plan auto-starts (above) and the hero renders that chapter directly; the cold-start path also avoids the old five-choice picker entirely and the 'choose your path' sheet is deferred until AFTER a first completed reading. | src/screens/HomeScreen.tsx:977-993, src/utils/coldStart.ts:214-222, src/App.tsx:255-258 |
| Audio on the same card as the reading | The hero photo plate IS the play button, sitting directly above the same card's reading deck and the expanded chapter text; audio is real ESV human-reader audio, chains chapter to chapter, and is pre-warmed in the background. No separate audio section exists. | src/screens/HomeScreen.tsx:2313-2320, 2402-2405, 2640-2725, 954-961 |
| The reading is already open on arrival | A dedicated effect seeds today's chapter expanded and fetched on every mount for the four returning personas, deliberately bypassing handleRead so it credits nothing. The hero's Read button therefore reads 'Hide' on arrival. | src/screens/HomeScreen.tsx:1440-1456, 2540 |
| One nudge a day, neutral wording, never "your streak is about to die" | All seven push templates are plain scripture invitations ('Your Daily Reading', 'Today's reading: {passage} — "{verse}"'). Nothing mentions the streak, a broken run, or lost progress, and a last_sent_date ledger keyed to the subscriber's local date enforces exactly one send per day with a 2-hour catch-up window. | netlify/functions/push-send.js:121-138, 256, 280-293, 325 |
| Momentum cues: streak and day-count | Both are present and prominent for this persona: a tabular 'N days' counter with an encouragement label in the header (tappable to a milestone modal at 7+), and the greeting itself prints 'Day N — keep going' / 'N days strong!'. Hidden only for new_to_faith and comfort. | src/screens/HomeScreen.tsx:2006-2045, src/utils/persona-config.ts:237-241, src/utils/i18n.ts:579-600 |

### Refuted by the verifier

- [partial] Closing the "I meant to" gap (62% wish they read more)
  Refutation: The auditor's own evidence file contradicts the claim: when the browser has no service worker, PushOptIn's enable() does not fall through to nothing — it calls openCalendarReminder(hour), which opens a recurring calendar-reminder URL (src/components/PushOptIn.tsx:51 → src/utils/push.ts:250-258). So the no-service-worker case does have a second return mechanism; the claim that "there is no other return mechanism" for that case is false. (For a user who actively declines/dismisses the push ask with a working service worker, no separate return mechanism exists, so the "declines" half of the claim is closer to accurate — but the claim as stated bundles both cases as equally unmet, which the code refutes for the no-SW case.)

## Deeper study (persona 3)

**Door:** Title: "I want to go deeper in the Word" (i18n.ts:929 path_study_head). Promise: "Commentary, original languages, cross-references." (i18n.ts:930 path_study_promise). CTA: "Open the study reading" (i18n.ts:931). Arrival strip: "You're in the study reading. Commentary is open below, with Compare and the original languages." (i18n.ts:915 path_arrival_study).

**What opens:** sectionOrder is ['greeting','hero_audio','plan_scripture','scripture','commentary','ai_prompt'] (persona-config.ts:361-368), but the rendered page interleaves ungated blocks. Top to bottom, with an active plan: greeting row with the streak counter, path swatch, language and theme toggles (HomeScreen.tsx:2004-2052) and HomeContextChips (1987); PathArrivalStrip once (2093-2101); the Sermon Notes banner (2106) — above the reading; the hero photo plate with plan label "PlanTitle — Day N", date, chapter title, translation caption, prev/next-day chevrons and a full-plate play button (2261-2360), a control row of Read/Hide + Restart + translation chips + a Compare chip (2516-2624), then the chapter text seeded already open (1450-1456), InlineReflection and "Mark as read" (2693-2716); PWAInstallBanner (2846); EmailNudgeCard (2848); the plan-setup wizard while no plan exists (2876-2884); TODAY'S STUDY, which prints the same chapter again behind its own Read button plus the Greek/Hebrew toggle (3222-3350); the Commentary card, already expanded (3352-3410); TODAY'S CHAPTERS / ADDITIONAL READING (3434+); the Daily Quote (3917); Word of the Day (3952-3954); the For You personalization card (4163-4225); the Bible AI prompt card (2918); PromoAds (4582). With no plan there is no scripture at all: the hero shows "Choose your reading plan / Browse plans" (2208-2244), the wizard card asks again (PastorStudyOnboarding.tsx:268-296), and the scripture section prints "No reading plan active / Browse plans" (HomeScreen.tsx:3519-3527).

**Verdict:** No — the live deeper_study path delivers the audio placement and note/highlight sync the research asks for, but it inverts the two needs that define the persona: commentary is open by default and the original-language tool is unreachable without a plan, day 0 asks for a plan three times with no scripture on screen, the same chapter prints twice, and the habit counter measures app opens and prints a Day 1 reset beside a plan on Day 15 — the shame cue this path's own research bans.

Claims verified: 16, held 13, refuted 3.


### Contradicted (the live code does the opposite)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Text over commentary and low clutter — the recurring YouVersion complaint is "it keeps adding functionality, it's getting so cluttered" | The opposite. Commentary arrives expanded because commentaryExpanded is initialised from the persona flag, so the "Tap to read" affordance never renders for this path. Around the reading the page stacks the Sermon Notes banner above it, a PWA install banner, an email nudge, a second full card printing the same chapter, the Daily Quote, Word of the Day (wordOfDay: 'full'), a For You personalization card and PromoAds — roughly sixteen blocks on a path whose stated need is low clutter. | src/screens/HomeScreen.tsx:468, src/utils/persona-config.ts:396, src/screens/HomeScreen.tsx:3363, src/screens/HomeScreen.tsx:2106, src/screens/HomeScreen.tsx:2846-2848, src/screens/HomeScreen.tsx:3917, src/screens/HomeScreen.tsx:3952-3954, src/screens/HomeScreen.tsx:4163, src/screens/HomeScreen.tsx:4582 |
| Depth tools when wanted, not by default | Inverted on both halves. Commentary is on by default (commentaryExpanded seeded true from commentary: 'expanded'), while the one depth tool the reader would summon — Greek/Hebrew — has exactly one persistent call site, inside the plan_scripture block that returns null when there are no plan passages, so it cannot be turned on at all without an active plan. Compare and the translation chips, meanwhile, sit permanently in the hero control deck rather than behind a tap. | src/screens/HomeScreen.tsx:468, src/utils/persona-config.ts:396-397, src/screens/HomeScreen.tsx:3223-3225, src/screens/HomeScreen.tsx:3233-3247, src/screens/HomeScreen.tsx:2580-2624 |
| Day 0: choose a plan, two or three at most: chronological, whole-Bible-in-a-year, and a Futures series plan. Dated, resumable. | Day 0 asks three times on one screen and shows no scripture at all. heroChapterRefs floors comfort, new_to_faith and pastor_leader with a rotating chapter but has no deeper_study branch, so the hero renders "Choose your reading plan / Browse plans", the wizard card asks again, and the scripture section prints "No reading plan active / Browse plans" below that. When the wizard does offer plans it lists six computed by category filter, not two or three, and no chronological plan exists (through-bible-year is Genesis-to-Revelation canonical order). The plan that results is dated but not resumable. | src/screens/HomeScreen.tsx:1302-1315, src/screens/HomeScreen.tsx:2208-2244, src/components/PastorStudyOnboarding.tsx:268-296, src/components/PastorStudyOnboarding.tsx:299, src/screens/HomeScreen.tsx:3519-3527, src/data/plans.ts:269-279 |
| Every day: the passage first, text-only, 10 minutes. Commentary, Compare and original languages collapse behind one "Study" tap (they are open by default today). Audio toggle on the passage card. | Still open by default, and the passage is not first. Commentary renders expanded (468 from persona-config 396); Compare and the translation chips live permanently in the hero control deck; the Greek/Hebrew toggle is a third separate control in the plan-card header, with hardcoded English strings even though the path ships es/pt/id. Nothing collapses behind a named Study tap — grep for a studySheet flag returns nothing. And the Sermon Notes banner renders above the hero on every path, against its own definition comment which states that for the four returning personas "nothing sits above today's reading". Only the audio half is right: the hero plate and each plan card carry their own Listen. | src/screens/HomeScreen.tsx:468, src/screens/HomeScreen.tsx:2580-2624, src/screens/HomeScreen.tsx:3233-3247, src/screens/HomeScreen.tsx:3245, src/screens/HomeScreen.tsx:1774-1783, src/screens/HomeScreen.tsx:2106, src/screens/HomeScreen.tsx:3266-3290 |
| Habit: a chain counter with identity language ("you read five days most weeks") and a reset without shame. | The counter measures app opens, not readings: recordStreakToday() runs in a mount effect with empty deps, and the greeting prints "Good morning, {name}. Day {streak}." from that same number. On a reset morning the streak===1 branch prints "Day 1 — your longest run is {best} days" while the plan card beside it still reads Day 15 — three day-numbers on one screen, one of them a reset, in the app's own voice. A milestone modal with a 56px flame auto-pops at 7/14/30 opens. Identity language exists nowhere; the nearest string is "You're building a strong daily habit", gated on readsDaily, which is derived from UTC-stamped event dates so an evening reader west of UTC is counted on the wrong day. The grace that does exist is a silent one-per-week freeze and the best-run acknowledgement — never a one-tap repair. | src/screens/HomeScreen.tsx:785-795, src/screens/HomeScreen.tsx:173, src/utils/persona-config.ts:242-245, src/utils/persona-config.ts:114-120, src/utils/i18n.ts:425, src/screens/HomeScreen.tsx:4441-4457, src/utils/behavior.ts:53, src/utils/personalization.ts:140, src/utils/streak.ts:73-77 |

### Defects (the path breaks its own promise)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| The door promises cross-references; no cross-reference surface renders anywhere in the live app | severity: high | src/utils/i18n.ts:930 "Commentary, original languages, cross-references." The study layer does expose them (src/utils/study.ts:32 crossRefs) but the only consumer is src/components/preach/PrepSheet.tsx:403-405, mounted solely by src/screens/PreachScreen.tsx:263, and PreachScreen is referenced nowhere outside its own test file — src/components/TabBar.tsx:23-27 routes home/journal/messages/plans/more only. Nothing in HomeScreen imports fetchStudyPassage. |
| Tapping Read credits the plan day, contradicting the settled intent stated forty lines above it | severity: high | src/screens/HomeScreen.tsx:754 calls markPlanDayComplete(passage) inside handleRead; the comment at src/screens/HomeScreen.tsx:713-716 states "Deliberate completion... Replaces auto-marking on open (focus-group: completion needs intent)" and handleMarkRead at src/screens/HomeScreen.tsx:717-731 is the deliberate path. Expanding a chapter therefore advances the plan and can fire the one-shot finish celebration (src/screens/HomeScreen.tsx:755). The arrival seed at src/screens/HomeScreen.tsx:1450-1456 correctly bypasses handleRead, which makes the credit on the manual tap the anomaly. |
| The same chapter is printed twice on one screen, both copies expanded | severity: medium | heroChapterRefs is built from todaysPlanPassages first (src/screens/HomeScreen.tsx:1291-1297) and the arrival seed expands it (src/screens/HomeScreen.tsx:1450-1456); the plan_scripture block then renders every todaysPlanPassages entry again with its own header, Listen and Read button (src/screens/HomeScreen.tsx:3249-3350), and isReadingOpen (src/screens/HomeScreen.tsx:1319-1328) collapses chapter and verse-range refs to the same chapter, so the second copy reads as already open too. The scripture section has a dedupe guard for exactly this (slotsOnlyMode, src/screens/HomeScreen.tsx:3446-3450); the plan section has none. |

### Missing

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Journey — Together: "read with" one person, a spouse or a friend; both see each other's day-count, nothing else | Nothing in the code does this. Grepping src/ for "read with", "read_with", "readWith", "reading partner" and "accountability" returns no product code (only an unrelated repair comment in staffIdentity.ts and a commentary string). There is no pairing, invite, or shared-count surface anywhere; the only cross-person numbers in the repo are the pastor's campus analytics and two seeded counter sections that are never mounted. | src/utils/persona-config.ts:361-368, src/sections/index.ts:8, src/sections/index.ts:10, src/screens/HomeScreen.tsx:3411-3414 |

### Partial

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| A dated plan | Dated, yes: the catalogue carries Through the Bible in a Year (365), New Testament in 90 Days and Psalms & Proverbs (181), and the plan day is computed from calendar days since startedAt on a local-midnight axis. But the function's own comment says it "advances automatically each day regardless of completion", so a reader who misses three days returns to Day 15 with Days 12-14 never served; only a manual prev/next-day chevron on the hero recovers them, and that offset is date-keyed so it resets at midnight. At the end the day clamps at totalDays, so the final chapter is served every morning forever while the finish celebration is one-shot behind finishedCelebrated. | src/data/plans.ts:237-279, src/screens/HomeScreen.tsx:124-140, src/screens/HomeScreen.tsx:885, src/screens/HomeScreen.tsx:817-824, src/screens/HomeScreen.tsx:703-708 |
| Journey — Print: a "read like paper" mode, larger type, no chrome | Larger type exists and persists (FontSizeControls with a stored scriptureFontSize passed into every ScripturePassage), and the reading surface deliberately has no internal scrollbar or max-height so the chapter scrolls as one page. There is no chrome-free mode: the control deck, chips, section headers, sticky action bar and tab bar all stay, and no grep hit exists for a paper/reader/focus mode. The reading panel is also hardcoded to #FFFFFF with forced dark-ink CSS-var overrides, so it ignores the app's dark theme. | src/components/FontSizeControls.tsx:13-31, src/screens/HomeScreen.tsx:203, src/screens/HomeScreen.tsx:2626-2643, src/screens/HomeScreen.tsx:2678-2684 |

### Delivered

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Audio as a companion, on the passage, not in a separate section | Audio is attached to the passage itself, never a separate section: the whole hero photo plate is one play button and the centred labelled control sits on it, the sticky reading bar carries Listen/Pause for the open chapter, and each plan passage card in TODAY'S STUDY has its own Listen button beside the chapter heading. There is no audio section in sectionOrder. | src/screens/HomeScreen.tsx:2318-2328, src/screens/HomeScreen.tsx:2497-2499, src/screens/HomeScreen.tsx:3266-3290, src/components/ReadingActionBar.tsx:31-37, src/utils/persona-config.ts:361-368 |
| Notes and highlights that sync reliably | Highlights and journal entries are both in the cloud-sync key set and both have real merge strategies: highlights union-merge so an offline device's un-pushed highlights survive a pull, and the journal merges by entry id with tombstones rather than being overwritten. Verse notes write through the journal. Caveat: syncOnStartup no-ops without an email, so a reader who never gives one stays localStorage-only. | src/utils/cloudSync.ts:16-17, src/utils/cloudSync.ts:321-333, src/utils/cloudSync.ts:414-422, src/utils/cloudSync.ts:606, src/utils/storage.ts:29 |

### Refuted by the verifier

- [defect] The path's headline feature, original languages, is unreachable without an active plan, while the door and the arrival strip both promise it
  Refutation: The auditor missed a second live call site: src/components/HighlightToolbar.tsx:177-180 also calls setGreekHebrewMode, gated only on !basicMode (deeper_study has highlighting:'full' so basicMode=false), and it is rendered globally for any text selection whenever pf.highlighting !== 'none' (HomeScreen.tsx:4566). A deeper_study reader with reading slots but no active plan (todaysPlanPassages.length===0) gets no plan_scripture toggle, but can select text in the reading-slots section (HomeScreen.tsx:3434+, ScripturePassage-rendered) and reach Greek/Hebrew via that toolbar toggle -- so "on reading slots... can never turn it on" is false.
- [defect] The greeting's "Day N" counts app opens, and resets to Day 1 with a longest-run line beside a plan that reads Day 15
  Refutation: recordStreakToday() at src/utils/streak.ts:41-55 is idempotent per calendar day (early-returns isNew:false if raw.lastDate already equals today), so the mount effect at HomeScreen.tsx:785-793 does not increment "Day N" on every app open — only once per new calendar day of actual use, correctly reset to 1 on a missed day beyond the freeze window. The reset/longest-run and dual plan-Day-N mechanisms cited are real, but the "counts app opens" characterization of the defect is false.
- [defect] Commentary arrives expanded on the path whose stated need is text over commentary, and its collapse affordance never renders
  Refutation: deeper_study's own description at src/utils/persona-config.ts:384 is 'Original languages, commentary, depth' — commentary IS its stated need, not something subordinate to text — so features.commentary:'expanded' at line 396 delivers exactly what this persona is documented to want. The onClick at HomeScreen.tsx:3358 only expanding and 'Tap to read' at 3363 being unreachable are accurate, but the claim mischaracterizes this as a defect by inventing a 'text over commentary' need that appears nowhere in this persona's (or any persona's) config.

## Pastor and leader (persona 5)

**Door:** Title: "I'm a pastor and I teach and preach" (src/utils/i18n.ts:932, key path_leader_head). Promise: "Study tools and sermon prep. Pastors sign in for campus tools." (src/utils/i18n.ts:933, key path_leader_promise). CTA: "Open my study" (src/utils/i18n.ts:934). Door registered at src/utils/choosePath.ts:34; card icon Feather (src/components/ChoosePathSheet.tsx:46). One-time arrival strip: "You're in the pastor study. Today's chapter is open with commentary, and Between You & God sits below it. Preach is the card above." (src/utils/i18n.ts:913).

**What opens:** sectionOrder for pastor_leader is ['greeting','hero_audio','plan_scripture','scripture','pastoral_prompt','commentary','ai_prompt','campus_count','book_cards'] (src/utils/persona-config.ts:439-449) but it is ONLY ever used as a membership test (`personaConfig.sectionOrder.includes(...)` at HomeScreen.tsx:2833, 2876, 2918, 3222, 3434) — it never orders anything, so the live DOM order differs from the declared order and 'campus_count' resolves to no mounted section at all. Live top-to-bottom: (1) header row — campus chip, streak chip "N days" + encouragement line (HomeScreen.tsx:2006-2045), PathSwatch, language, theme; (2) greeting "Good <tod>, <name>. Day N. This time matters." + Search (HomeScreen.tsx:2054-2075, persona-config.ts:246-253); (3) PathArrivalStrip once (2096-2104); (4) the Preach card — eyebrow "Pastor study", title/sub from preach_card_title/sub, tap leaves the app for pastors-sermon-prep.netlify.app (1786-1830, mounted at 2106) — this is the FIRST card, above the reading; (5) hero photo card: chapter caption, date, play, chapter pills, Read/Hide, Compare + translations, and today's chapter already expanded with InlineReflection and "Mark as read" (2120-2800, arrival seed at 1449-1456); (6) Between You & God prompt with Journal This / Talk It Through (2833-2843 → PastoralReflectionSection.tsx:28-78); (7) PWA banner + email nudge (2847-2849); (8) pastor study wizard until completed (2876-2884 → PastorStudyOnboarding.tsx:221-265, incl. the "what are you preaching" focus field); (9) Bible AI card "Bible AI for sermon prep", hidden forever after the first AI use (2918 → BibleAIPromptSection.tsx:12, 34-38); (10) book cards Grace & Truth / No More Fear (2921-2967); (11) TODAY'S STUDY — plan passages with the Greek/Hebrew toggle, rendered only when a plan is active (3222-3266); (12) Commentary, expanded by default (3355-3414); (13) TODAY'S CHAPTERS / ADDITIONAL READING for manual slots (3434-3460); (14) daily quote, Word of the Day, Sunday weekly review (3900-3964); (15) Your campus card, active plans strip, For You (3973, 4090s, 4163-4226); (16) Campus Overview — real campus analytics behind a pastor code, else the code prompt (4235-4320).

**Verdict:** Roughly half of it: the pastor door opens on a real study — chapter already open with commentary, a personal reflection card, Compare and tappable Greek/Hebrew, four sermon quick-prompts and honest campus numbers — but the research's three named additions are not live (no source-and-licence inline, no time-saved, no export step; the built Prep/Outline/Publish workspace and the 11-table study layer are unmounted code), and the path prints numbers it has not earned: "Day N" counts opens, "days this week" is derived from it, and the default Commentary is model text generated by a prompt that asks for a cross-reference.

Claims verified: 20, held 18, refuted 2.


### Contradicted (the live code does the opposite)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Trustworthy sourcing — no invented references; scripture and study results come from the verified tables, never the model | Scripture text is fetched, not generated — but on any day outside the ~20-chapter curated set the pastor's expanded Commentary card is model output, and the prompt that generates it explicitly instructs the model to "surface one original-language or cross-reference insight". It is then inserted into allCommentaries as a peer source alongside Matthew Henry and rendered in the same tab strip, labelled only "AI INSIGHT". The app asks for the fabricated cross-reference the research warns about and prints it where commentary goes. | src/screens/HomeScreen.tsx:1462-1466 (not 922-926, which is unrelated plan-loading code) |
| Trust panel: source and licence inline on every commentary, cross-reference and lexicon result — "visible, not hidden in Settings" | The licence panel exists and is literally a Settings card, pastor-only, rendered from MoreScreen — the exact placement the research rules out. Its own header says "StudySourcesCard — Settings card". Inline, nothing carries a licence: the Commentary card prints a bare source name, and the lexicon popup discards the study layer's source_id and shows no source line at all. | src/screens/MoreScreen.tsx:915, src/components/StudySourcesCard.tsx:1-8, src/screens/HomeScreen.tsx:3396-3402, src/components/GreekHebrewPopup.tsx:38-52, src/utils/study.ts:20 |

### Defects (the path breaks its own promise)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Tapping Read credits a plan day. handleRead calls markPlanDayComplete, so merely opening (or re-opening) a chapter marks the plan day complete and can fire the plan-finish celebration — the deliberate "Mark as read" path that was built for intent is bypassed. Both the hero Read button and the TODAY'S STUDY Read button go through it. | severity: high | src/screens/HomeScreen.tsx:733-758 (markPlanDayComplete at 754), call sites src/screens/HomeScreen.tsx:2518, 3334, 3607, 3674; the deliberate path is src/screens/HomeScreen.tsx:717-731 |
| The Sunday weekly review, which only this path sees, prints a fabricated "days this week": daysRead is Math.min(streak, 7) off that open-counting streak, rendered as a hard number beside "day streak". | severity: medium | src/screens/HomeScreen.tsx:106-119 (daysRead at 115), src/components/WeeklyReviewCard.tsx:44-45, src/utils/persona-config.ts:457, src/screens/HomeScreen.tsx:3958-3963 |
| The door promises "Study tools", but the labelled Greek/Hebrew switch is inside the plan-gated block: plan_scripture returns null when todaysPlanPassages is empty, which is precisely the state of a pastor arriving on the PASTOR_CHAPTERS floor with no plan. The only other way in is the verse-selection toolbar. | severity: medium | src/screens/HomeScreen.tsx:3222-3225, src/screens/HomeScreen.tsx:3233-3248, src/screens/HomeScreen.tsx:1309-1314, src/components/HighlightToolbar.tsx:176-181 |
| The hero's headline names the wrong chapter once he moves. The 27px caption is hard-wired to allLabels[0] while the translation caption, the Read button and audio all follow heroChapterIndex — on a multi-chapter plan day (through-bible-year, new-testament-90) the plate says chapter 1 while he is reading another, and the same chapter is then printed a second time in TODAY'S STUDY below. | severity: medium | src/screens/HomeScreen.tsx:2302-2304, src/screens/HomeScreen.tsx:2311-2315, src/screens/HomeScreen.tsx:2517-2518, src/screens/HomeScreen.tsx:3249-3266 |

### Missing

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Time saved: show it. Minutes from open to outline, per week. | Nothing measures elapsed time anywhere: grep for timeSaved / time_saved / "minutes saved" across src returns nothing, and no event carries a duration (behavior.ts records type/date/ts/meta only). The only weekly surface for this path prints two day-counts derived from the streak, not time. | src/utils/behavior.ts:20-26, src/utils/behavior.ts:48-60, src/screens/HomeScreen.tsx:106-119, src/components/WeeklyReviewCard.tsx:44-45 |
| Export as its own step: outline to slides and to congregation notes, separate from drafting | The whole export half of the app is built and unreachable: PrepSheet, OutlineBuilder, PublishSermon and SermonArchive are mounted only by PreachScreen, whose own header records "Not mounted from App or Home as of 9 Sep 2026", and App.tsx never imports it. The live Home has no export control; the Preach tap navigates off-app with an empty payload. | src/screens/PreachScreen.tsx:1-10, src/screens/PreachScreen.tsx:19-24, src/App.tsx:92, src/App.tsx:418, src/screens/HomeScreen.tsx:1789 |

### Partial

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Faster research and brainstorming without the scholar's workload | Exists: four pastor-only quick prompts ("For your message" — three teaching angles / find an illustration / Greek or Hebrew behind this word / turn my highlights into an outline) built from the verse in front of him, his preaching focus and his filed captures, and sent to the model in one tap. Falls short: they live only on Bible AI's empty state, so they vanish the moment a thread starts; the Home card that advertises them self-destructs after the first AI use ever; and the 11-table study layer that would answer without the model (fetchStudyPassage, fetchStudyCommentary, searchIllustrations, fetchLectionary) is imported by exactly one component, PrepSheet, which only lives inside the unmounted PreachScreen. | src/components/BibleAI.tsx:792-827, src/components/BibleAI.tsx:304-322, src/utils/pastorPrompts.ts:78, src/sections/BibleAIPromptSection.tsx:12, src/components/preach/PrepSheet.tsx:24, src/screens/PreachScreen.tsx:1-5 |
| Time relief | Real savings at arrival only: the chapter opens itself, a floor chapter is always present, commentary arrives expanded, and one tap files a verse to the sermon bag. But the loop dead-ends: the filed bag (dw_sermon_prep) has no mounted reader — its only renderers are SermonWorkspace's "My Preparation" and OutlineBuilder, neither of which App.tsx mounts — and the Preach tap does window.location.assign(PREP_URL) carrying nothing, so Sunday starts empty in the other app. | src/screens/HomeScreen.tsx:1449-1456, src/components/HighlightToolbar.tsx:42-45, src/screens/HomeScreen.tsx:1786-1793, src/App.tsx:92, src/App.tsx:418 |
| Original-language and theological support | Exists: greekHebrew 'full' for this path; in Gk/Heb mode every word is tappable, resolved locally against the passage's Strong's map and opened in the lexicon popup (STEPBible CC-BY lexicons first, strongs.js fallback), with a Compare-translation column and curated Matthew Henry / Thayer's commentary. Falls short: the only labelled switch ("Tap for Greek/Hebrew") sits inside the plan_scripture block, which returns null when no plan is active — the day-one pastor's exact state — leaving only the verse-selection toolbar toggle, which he has to discover by selecting text. | src/utils/persona-config.ts:452, src/screens/HomeScreen.tsx:3233-3248, src/screens/HomeScreen.tsx:3222-3225, src/screens/HomeScreen.tsx:1715-1757, src/components/GreekHebrewPopup.tsx:38-52, src/components/HighlightToolbar.tsx:176-181 |
| Illustrations and repurposing | Illustrations: one AI prompt ("Find an illustration for this idea") that asks the model to invent one; the study layer's own illustrations table has a client function (searchIllustrations) that no mounted component calls. Repurposing: nothing — no outline-to-anything path in the live app; the nearest thing is "Save to notes", which dumps the AI thread into the journal. | src/utils/i18n.ts:809, src/utils/study.ts:89, src/components/BibleAI.tsx:388-389, src/components/BibleAI.tsx:578-589 |
| Framing: the AI is a research assistant, never the writer — "study aid" copy, not "AI writer" | No "AI writer" copy exists: the Home card says "Bible AI for sermon prep — Get teaching angles, illustration ideas, and deeper context", the system prompt casts the model as "a sharp, experienced ministry partner", and the panel is a "Bible study assistant". But nothing on any surface states the assistant is not the writer, the pinned outline prompt asks the model to produce the sermon shape ("Big idea, three points, one weekly action"), and the Between You & God button feeds "what's on my heart" into a persona whose prompt answers with "vision, strategy, and direction — not feelings". | src/sections/BibleAIPromptSection.tsx:34-38, src/utils/persona-config.ts:496, src/components/BibleAI.tsx:252, src/utils/i18n.ts:812, src/components/PastoralReflectionSection.tsx:66 |
| The pastor study already exists: chapter with commentary, Preach card, sermon congregations, notes email | Chapter + commentary + Preach card + the wizard's preaching-focus field are all live. Two parts of it are not this path's any more: the Preach card's congregation chooser is replaced by the off-app deep link for pastor_leader only, and the sermon-notes tab that holds congregation notes and the notes email is absent from the tab bar, reachable only by Sunday deep link. Also, with a plan active the same chapter is printed twice — the hero caption and the TODAY'S STUDY card both name it and both offer Read. | src/screens/HomeScreen.tsx:1789, src/components/TabBar.tsx:22-27, src/App.tsx:418, src/screens/HomeScreen.tsx:2302-2304, src/screens/HomeScreen.tsx:3249-3266 |

### Delivered

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Affordable access after the Logos subscription backlash — no forced recurring cost | There is no pricing, paywall, tier or payment code anywhere in the app. A grep for stripe/checkout/subscription across src and netlify/functions returns only web-push subscription handling (src/utils/push.ts:102-131). Every pastor feature is gated on persona or a campus code, never on payment (src/utils/persona-config.ts:450-465; HomeScreen.tsx:1101). | src/utils/persona-config.ts:450-465, src/screens/HomeScreen.tsx:1101, src/utils/push.ts:102-131 |
| Arrival: the chapter is already open — no decision to make | A persona-gated effect seeds today's chapter expanded and fetches it on load, deliberately bypassing handleRead so nothing is credited ("The seed must NOT go through handleRead"). With no plan and no reading slots the pastor still gets a chapter: PASTOR_CHAPTERS rotates a leadership chapter as a day-one floor. | src/screens/HomeScreen.tsx:1441-1456, src/screens/HomeScreen.tsx:1309-1314 |
| Between You & God — the pastor's own reflection, not ministry admin | A dark reflection card renders directly after the hero for this path only, cycling one of 15 prompts per day, with Journal This (prefills the journal) and Talk It Through (opens Bible AI with context). | src/screens/HomeScreen.tsx:2831-2843, src/components/PastoralReflectionSection.tsx:28-78, src/utils/persona-config.ts:473-489 |
| Campus tools promised by the door do not invent numbers | Campus Overview fetches /api/analytics-dashboard with the pastor's own X-Pastor-Code and renders only the campus-scoped shape the API returns; without a code it shows the code prompt, never a number. The old seeded counts were removed for exactly this reason, and the seeded CampusCountSection is not imported by HomeScreen (only BibleAIPromptSection and ComfortVerseBannerSection are). | src/screens/HomeScreen.tsx:1080-1135, src/screens/HomeScreen.tsx:4231-4320, src/screens/HomeScreen.tsx:35 |

### Refuted by the verifier

- [defect] "Day N" counts app-opens, not readings. recordStreakToday() runs in a mount effect with empty deps, so every Home mount advances the count that the pastor greeting ("Day N. This time matters." / "N days. You're leading from a full cup."), the header chip, the done card and the milestone overlay all print. The same screen deliberately deleted seeded campus numbers because "pastors were quoting invented engagement numbers".
  Refutation: recordStreakToday() (src/utils/streak.ts:41-56) is explicitly idempotent per local calendar day — it compares the stored lastDate against today and short-circuits with isNew:false if already recorded today, so repeated Home mounts on the same day do NOT advance the count. The claim that "every Home mount advances the count" is false; only the first open of a new calendar day advances it, matching normal reading-day semantics, not app-open counting.
- [defect] The first card on the pastor's Home is a door out of the app, above the reading, and it carries nothing. sermonNotesRow is mounted once, directly under the greeting, contradicting its own spec comment ("for the four returning personas it renders BELOW the reading — nothing sits above today's reading"); for pastor_leader the tap does window.location.assign(PREP_URL) with no filed captures and no preaching focus attached, and the bag it would carry has no mounted reader in this app.
  Refutation: The code does exactly what the claim describes mechanically, but the "contradicts its own spec comment" framing is wrong: the auditor quotes the stale 1-Sep comment (HomeScreen.tsx:1775-1779) while skipping the comment immediately below it at 1783-1785 ("pastor_leader is the exception (Ashley, 9 Sep 2026): this card leaves Daily Word for Pastors Sermon Prep"), which is a dated, explicit ruling superseding the old spec — and a dedicated test suite (src/screens/home-pastor-floor.test.ts, describe block "pastor Preach card deep-links to Pastors Sermon Prep (Ashley, 9 Sep 2026)") pins this exact window.location.assign(PREP_URL) behavior as intended, not accidental. This is decided behavior, not a "defect."

## Comfort (persona 6)

**Door:** Title: "I'm going through something hard" (i18n.ts:935 path_comfort_head). Promise: "Comfort scriptures. Nothing else asked of you." (i18n.ts:936 path_comfort_promise). CTA: "Open comfort" (i18n.ts:937). Arrival strip after the pick: "You're in comfort. Just the scriptures. Nothing else is asked of you." (i18n.ts:917 path_arrival_comfort, rendered by PathArrivalStrip.tsx:20,59-60).

**What opens:** persona-config.ts:510-519 declares sectionOrder = greeting, comfort_verse_banner, hero_audio, scripture, comfort_scripture, ai_prompt, comfort_card, book_cards — but HomeScreen only gates on comfort_verse_banner, comfort_scripture and ai_prompt (no grep hit anywhere for 'hero_audio', 'greeting', 'comfort_card' or 'book_cards'), so the real order is the JSX order. Live, top to bottom: (1) header row — "Daily Word", campus chip, path swatch, language switch, theme toggle, with the streak badge correctly suppressed (HomeScreen.tsx:2006); (2) the rotating presence greeting plus a Search button (HomeScreen.tsx:2055-2091, searchEnabled true at persona-config.ts:530); (3) the one-shot path arrival strip (2095-2103); (4) the dark Sermon Notes card "THIS WEEK · Sermon Notes →" (2106, built at 1786-1830) — above the reading; (5) the full hero: photo carousel that is itself a play button, date, today's comfort chapter title, translation pills, expandable chapter, an InlineReflection journal box and a green "Mark as read" button (2120-2760, comfort chapter injected at 1302-1305); (6) date-navigation chevrons on a charcoal panel (2768-2823); (7) PWA install banner (2846); (8) the comfort verse banner with its guided-prayer button — first in sectionOrder, eighth on screen (2887); (9) ComfortSection "A WORD FOR YOU TODAY", the same chapter again with its own translation picker and Listen button, "Reading x of y", "I've finished reading", then the ask chain (2892-2911); (10) the Bible AI card (2918); (11) two book promo cards, Grace & Truth and No More Fear (2921-2969); (12) the daily quote (3918); (13) PromoAds house ads — books and the college offer (4582). "scripture" returns null (3434-3442), For You is hidden (4163), the campus counter is hidden (campusCount 'hidden', gate at 3973).

**Verdict:** The comfort door keeps its promise in its words and its content — the presence greeting, the Isaiah 41:10-led verse rotation, the guided prayer, no graduation prompt, no push ask, no path gate, no header streak — but not in its behaviour: the live path counts opens into a streak and celebrates it with a full-screen flame, prints a reading count and two completion buttons, asks a crisis reader to commit to a daily quota, renders today's chapter twice, sells two books and a college on the same screen, and carries none of the safety the research adds (no crisis-language flow, no region-correct line beyond one hardcoded US number on another tab, no disclosure, no campus-pastor route, no one-tap comfort from any other path, no next-day check-in).

Claims verified: 29, held 28, refuted 1.


### Contradicted (the live code does the opposite)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| No streak | recordStreakToday() runs in an empty-dependency mount effect on HomeScreen for every persona, so merely opening the app increments dw_streak_v2 for a comfort reader; the header badge is hidden, but the count is still being built and it still fires the celebration. handleMarkRead calls it again on the reading tap. | src/screens/HomeScreen.tsx:787 (mount effect), :723 and :729 (handleMarkRead); src/utils/streak.ts:41-89 |
| No streak — and no guilt-framed milestone | When the open-counted streak hits 7, 14, 30, 60, 100 or 365, the mount effect opens a full-screen black overlay with a 56px 🔥, "{x} days" in orange, "One week strong." and a "Keep going! 🙌" button. The overlay has no persona gate at all — it fires on the comfort path 600ms after the screen paints. | src/screens/HomeScreen.tsx:786-793, :4436-4483, src/utils/streak.ts:13, :89 |
| No count | ComfortSection prints "Reading {x} of {y}" in the header of the comfort reading card once a daily amount is set. (Two other counters were checked and do NOT fire here: the seeded campus counter is off because campusCount is 'hidden', and the For You block is persona-excluded.) | src/components/ComfortSection.tsx:93-100, src/utils/i18n.ts:393, src/screens/HomeScreen.tsx:3973, :4163 |
| No "complete" | The comfort path has two completion actions. The hero's expanded chapter ends in a green "Mark as read" button that calls handleMarkRead → recordStreakToday → DoneCelebration, which prints "{x} days and counting — you're building something." and a "🔥 {x}-day streak" line. ComfortSection's own flow ends in an "I've finished reading" button. | src/screens/HomeScreen.tsx:2702-2717, :717-731, src/components/DoneCelebration.tsx:29-34, :111-123, src/components/ComfortSection.tsx:195-203 |
| "Just the scriptures, nothing asked of you" — the path's own printed promise | After two chapters the app grades the reader and asks for a commitment: "You're doing great." followed by "Would you like to set a daily reading amount so we can have something ready for you each day?" with 1/2/3-chapter options, plus an earlier Yes/No "Would you like to read another passage from God's Word?" That is a quota question put to someone in crisis, on the screen whose arrival strip has just promised nothing would be asked. | src/components/ComfortSection.tsx:261-295, :297-356, src/utils/i18n.ts:712-715, :917, :936 |
| One passage | Today's comfort chapter renders twice on one screen. HomeScreen pushes COMFORT_CHAPTERS[localDayIndex()%30] into heroChapterRefs when a comfort reader has no plan and no slots, and then mounts ComfortSection, which independently computes the same index and renders the same chapter again with its own translation picker and its own Listen button. Separately, the anchor verse above the reading is drawn from a 16-entry list indexed independently of the 30-entry chapter list, so it is routinely from a different book than the chapter beneath it. | src/screens/HomeScreen.tsx:1298-1306, :2892-2911, src/components/ComfortSection.tsx:49-51, :81-146, src/sections/ComfortVerseBannerSection.tsx:13-30, :46 |
| Low stimulus | The comfort Home is the busiest surface the research path allows: a dark Sermon Notes card sits above the reading on this path as on every other, then a photo carousel hero with a play button and chapter slider, a charcoal date-navigation strip, a PWA install banner, and below the reading two book promo cards (Grace & Truth, No More Fear) plus the PromoAds house-ad block selling books and the college — the latter mounts because heroChapterRefs is never empty for comfort. The card that is first in this path's own sectionOrder renders eighth. | src/screens/HomeScreen.tsx:2106, :1786-1830, :2768-2823, :2846, :2887, :2921-2969, :4582, src/components/PromoAds.tsx:1-9, src/utils/persona-config.ts:510-519 |

### Defects (the path breaks its own promise)

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Opening the app builds a streak on the comfort path, and at day 7 it throws a full-screen 🔥 "One week strong. / Keep going! 🙌" overlay over a crisis reader | severity: high | src/screens/HomeScreen.tsx:785-795 (recordStreakToday in a mount effect, no persona gate; setShowMilestone after 600ms), src/screens/HomeScreen.tsx:4436-4483 (the overlay: 🔥 at fontSize 56, orange "{x} days", milestone_7, "Keep going!" + 🙌), src/utils/streak.ts:13, :41-89 (MILESTONES, count advances on open) |
| The path invites crisis disclosure into an LLM with no safety flow, no disclosure, and one hardcoded US-only crisis number buried on another tab | severity: high | src/sections/BibleAIPromptSection.tsx:39-43 ("You can ask anything — even the hard questions"), src/components/BibleAI.tsx:243-262 (system prompt assembly and send: no keyword scan, no confirm, no resource injection), src/screens/MoreScreen.tsx:838 (the only 988 line, hardcoded English, comfort-persona-gated at :785), src/screens/MessagesScreen.tsx:441-478 (prayer submit, no scan) |
| After two chapters the app grades the reader and asks them to commit to a daily quota — a direct contradiction of the door promise and the arrival strip it just printed | severity: high | src/components/ComfortSection.tsx:297-356 ("You're doing great." + comfort_set_daily_q + 1/2/3-chapter buttons), src/components/ComfortSection.tsx:261-295 (the earlier Yes/No "read another" ask), src/utils/i18n.ts:936 (path_comfort_promise "Nothing else asked of you."), src/utils/i18n.ts:917 (path_arrival_comfort "Nothing else is asked of you.") |
| "Mark as read" on the comfort hero fires a completion celebration that prints a day count and a 🔥 streak line on a path whose config hides the streak | severity: high | src/utils/persona-config.ts:113-114 (comfort's streak-hiding intent) plus src/screens/HomeScreen.tsx:717-729 and src/components/DoneCelebration.tsx:111-123 (unconditional streak line) — auditor's original pointers are accurate. |
| Today's comfort chapter renders twice on one screen, each copy with its own translation pills and Listen button, and the anchor verse above it is day-indexed independently so it usually names a different book | severity: medium | src/screens/HomeScreen.tsx:1298-1306 (hero fallback pushes COMFORT_CHAPTERS[localDayIndex()%30]), :2892-2911 (ComfortSection mounted below it), src/components/ComfortSection.tsx:49-51, :81-146 (same index, second picker at :103-121, second Listen at :128-146), src/sections/ComfortVerseBannerSection.tsx:13-30, :46 (16 verses vs 30 chapters, independent index) |

### Missing

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| From every path: a persistent one-tap "I need comfort now" that opens comfort without switching the saved path | There is no such control. The only way into comfort is the path chooser sheet, and picking it calls saveSetup({persona:'comfort'}), markPathAsked(), markPathArrival() and flushNow() — it changes and syncs the reader's saved path. The seam bar mounted above every screen carries only two links to futures.church, no comfort door. There is no ?comfort=1 deep link: App.tsx defines SERMON_DEEP_LINK and nothing comparable for comfort. | src/components/ChoosePathSheet.tsx:159-174, src/utils/choosePath.ts:30-35, src/components/Seam.tsx:43, :65, src/App.tsx:35 |
| Safety: crisis language typed into Bible AI or a prayer request → gentle confirmation → the region-correct line (988 US, 13 11 14 AU, 0800 543 354 NZ) | Nothing detects crisis language. BibleAI.tsx builds the system prompt and posts the message with no keyword scan, no confirmation step and no resource injection; the prayer form submits free text with no scan. A repo-wide grep for 988, 13 11 14, lifeline, hotline and suicid finds exactly one hit — the hardcoded US line on the More tab — and no AU, NZ, BR or ID number exists anywhere, so an Australian or Brazilian reader who is shown anything is shown the wrong country's number. | src/components/BibleAI.tsx:243-262, src/screens/MessagesScreen.tsx:441-478, src/screens/MoreScreen.tsx:838 |
| Disclose on first use that this is scripture and a church, not counselling | No disclosure exists on this path or in Bible AI. Grep finds no "not counselling"/"not counseling" string and no first-use notice; the comfort AI card's only framing is an invitation to ask anything. | src/sections/BibleAIPromptSection.tsx:39-43, src/components/BibleAI.tsx:243-262 |
| A co-created "when it's bad" card — who to call, what steadies you, a verse you chose | Nothing. Grep for dw_steady, steady_card and "when it's bad" returns no hits in src; the comfort journal config offers only the generic two prompts ("What's on your heart today?", "Write a prayer to God about what you're feeling") with no card, no contacts and no reader-chosen verse. | src/sections/ComfortVerseBannerSection.tsx:1-6, 39-48 (dead comfort_card key confirmed via persona-config.ts:517) |
| Next day: one quiet check-in, "still here, same verse or a new one" | There is no check-in of any kind. Comfort is excluded from push by persona, and push-send.js is persona-blind anyway: one global day-of-year passage from a 40-item list, templates in en and id only (no es/pt despite the app shipping those), and the copy is instructional — "Your Daily Reading", "Time in the Word", "Open your heart to {passage} today". Nothing in the app offers "same verse or a new one" on a return visit; the chapter simply advances with the local date. | src/App.tsx:311-312, netlify/functions/push-send.js:120-138, :141-149, :204-213, src/components/ComfortSection.tsx:49-51 |
| Dark and calm delivery | There is no comfort-specific surface treatment. The path inherits the global theme toggle, and its two own components paint pre-relook indigo gradients (rgba(92,107,192,…), #5C6BC0, #4A6070, #37474F) rather than a calm flat panel, while the screen around them carries a photo carousel, a #1C1410 sermon card and a charcoal date strip. | src/sections/ComfortVerseBannerSection.tsx:52-53, src/components/ComfortSection.tsx:180-183, :219, :132-134, src/screens/HomeScreen.tsx:1799-1802, :2768-2776 |

### Partial

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Presence and acknowledgement over problem-solving | The greeting, verses and prayer deliver presence (see delivered), but the same screen then puts three questions and a performance verdict to the reader: "Which words brought you the most peace?" with an "I've finished reading" button, then "Would you like to read another passage from God's Word?" with Yes/No, then after two chapters "You're doing great." plus a daily-quota question. The AI card invites the reader to bring the hardest thing ("You can ask anything — even the hard questions") with no presence-first prompts offered and no safety net behind it. | src/components/ComfortSection.tsx:184-204, :261-295, :297-356, src/sections/BibleAIPromptSection.tsx:39-43 |
| Positive religious coping; nothing that reads as "punishing God" or "have more faith" | The authored content is on register — 30 comfort chapters including lament (Lamentations 3, Psalm 42, Psalm 77) and devotions that frame God as present rather than demanding — and the comfort AI system prompt says lead with comfort before teaching, use scripture to bring hope not to lecture, keep it shorter and gentler, and encourage talking to a pastor. What is missing is any guard on the negative half: the prompt never forbids diagnosing, "have more faith" or a punishing-God framing, there is no tone tagging on the content, and nothing constrains the model when the reader types distress. | src/data/comfort.ts:3-11, src/utils/persona-config.ts:549-553, src/components/BibleAI.tsx:243-262 |
| Practical help and concrete answers, not platitudes | Exactly one concrete resource exists in the whole app: a pastoral-care card with a mailto:care@futures.church button and the line "Crisis support: 988 Suicide & Crisis Lifeline (call or text 988)". It is on the More tab, not on the comfort reading screen; it is gated on setup?.persona === 'comfort'; and its copy is hardcoded English with no i18n, so a Spanish, Portuguese or Indonesian reader gets English. | src/screens/MoreScreen.tsx:784-841, :806, :810, :838 |
| Slow audio first | Audio is prominent and first: the entire hero photo plate is a play button with a 56px labelled control on top of it, and ComfortSection carries its own Listen button. But there is no rate control anywhere in the repo (grep for playbackRate returns nothing), so "slow" is not implemented, and audio is not what the screen leads with in reading order — the Sermon Notes card sits above the hero. | src/screens/HomeScreen.tsx:2315-2327, :2352-2377, src/components/ComfortSection.tsx:128-146, src/screens/HomeScreen.tsx:2106 |
| Human next step: a prayer request to a real campus pastor | The only human route on this path is the More-tab mailto to one Atlanta address, with no campus routing and no named recipient; a mailto fails silently on a phone with no mail client, so the person never learns the message did not send. The app's other prayer surface is the public prayer wall on the Messages tab — it posts to a shared wall, has no private flag, demands an email address before the form opens, and is not linked from the comfort Home at all. | src/screens/MoreScreen.tsx:806-830, src/screens/MessagesScreen.tsx:432-435, :441-478 |

### Delivered

| Need or claim | Live behaviour | Evidence |
|---|---|---|
| Journey lead: no graduation prompt — the comfort reader is never nudged onward | UPGRADE_CONDITIONS holds only congregation→deeper_study; comfort has no entry and a comment forbids re-adding "Feeling Stronger?", so checkForUpgrade returns null and UpgradePromptCard renders nothing on this path even though HomeScreen mounts it. The stale comment at HomeScreen.tsx:2972 claims a comfort prompt exists; the code does not. | src/utils/pathway-upgrades.ts:40-51, src/utils/pathway-upgrades.ts:71, src/components/UpgradePromptCard.tsx:25-29, src/screens/HomeScreen.tsx:2972-2984 |
| Presence and acknowledgement in the words the reader meets first | The greeting for this path is a five-line date-keyed rotation of presence statements ("God is with you today", "You are not alone", "He holds you close", "Peace be with you", "You are loved") with no streak, no day number and no instruction, in all four languages; the comfort verse rotation leads on Psalm 34:18, Matthew 11:28-30, Isaiah 41:10, Psalm 46:1 — including the two verses the research names as dominant — and the guided prayer stays open until the reader closes it rather than auto-dismissing. | src/utils/persona-config.ts:254-262 (en), :144-153 (es), :181-190 (pt), :218-227 (id), src/sections/ComfortVerseBannerSection.tsx:13-30, :32-48, :40-43 |
| No guilt-shaped interruption on arrival | This path is excluded by name from the full-screen push opt-in and from the asked-once path screen, so a crisis arrival never meets a permissions screen or a "which path are you?" gate; the header streak badge and the For You behavioural block are both switched off for comfort; the "Choose Your Plan" funnel is switched off and the generic "scripture" section returns null rather than printing "No reading plan active". | src/App.tsx:311-312, src/utils/coldStart.ts:214-222, src/screens/HomeScreen.tsx:2006, :4163, :2854-2857, :3434-3442 |
| Local-date content rotation so the day's comfort word does not flip mid-evening | Both the comfort chapter and the comfort verse are indexed by localDayIndex(), an en-CA local calendar day index, not Math.floor(Date.now()/86400000). | src/components/ComfortSection.tsx:20-23, :49-51, src/sections/ComfortVerseBannerSection.tsx:45-48 |
| A verse-selection surface that does not turn comfort reading into study work | comfortMode strips Copy, Listen, Share and Go Deeper from the highlight toolbar, leaving Note and Close; commentary, Greek/Hebrew, word studies and the word-of-day are all 'hidden' for this path and the Greek/Hebrew toggle lives inside the plan_scripture block comfort never renders. | src/components/HighlightToolbar.tsx:164-185, src/screens/HomeScreen.tsx:4565-4578, src/utils/persona-config.ts:521-535, src/screens/HomeScreen.tsx:3222, :3235-3245 |

### Refuted by the verifier

- [defect] The comfort arrival is commerce and chrome before comfort: a Sermon Notes card above the reading, a date-nav strip, an install banner, two book promo cards and the house-ad block, with this path's own first section rendering eighth
  Refutation: The sermon card, date-nav, and install-banner ordering claims hold (HomeScreen.tsx:2106, 2764-2846), but the auditor's headline point — that book promo cards and the house-ad block precede comfort's own first section, pushing it to eighth — is backwards: comfort_verse_banner renders at HomeScreen.tsx:2887 (comfort's first sectionOrder item) BEFORE the book-cards block at :2921 and well before PromoAds at :4582, which mounts near the very end of the whole screen. So comfort's own content lands ahead of the book/ad chrome, not behind it.

## Live observations, 12 Sep 2026 evening (parent, browser)

- Production deploy: main e81b0dc1, Netlify state ready, 9 Sep. Page load 0.5 s, no console errors on any door.
- Fresh device lands on Day 1 of 40 with one Read button and a "Not new to faith? Choose your path" link. The sheet shows five doors.
- Comfort: greeting "He holds you close, friend", arrival strip "You're in comfort. Just the scriptures. Nothing else is asked of you.", then Sermon Notes card, then Matthew 11 with the AI pill, Search and the cookie banner around it.
- Deeper study: no passage on arrival. "Choose your reading plan", "Let's set up your study", "No reading plan active", a Bible AI teaser.
- Pastor: 1 Timothy 3 open with the seven-translation strip and Compare, Between You & God, a plan nudge, "Bible AI for sermon prep: get teaching angles, illustration ideas", COMMENTARY = "AI INSIGHT" only, no named commentary, "TODAY'S CHAPTERS: No reading plan active", campus overview behind a code.
- Congregation: 2 Timothy 1 (18 verses) printed twice on one screen (hero and TODAY'S CHAPTERS), no devotional thought from Ashley & Jane visible, plan shows Day 1 / 100.
- New to faith Day 1: headed "EPHESIANS 2:8-9 · ESV" but prints Ephesians 2:1-22, then a four-paragraph teaching, two questions, Mark Complete.
- Usage (Supabase, 12 Sep 13:29 UTC): 160 accounts, 12 synced in 7 days, 25 in 30 days; activity_events 3,132 rows, 52 in 7 days; app_open already carries the persona in `detail` (7 days: congregation 3 people, pastor_leader 5, deeper_study 2, new_to_faith 1); persona_change in 30 days: new_to_faith 2 people, deeper_study 5, pastor_leader 2, congregation 1, comfort 1; push_subscriptions 3 rows.
