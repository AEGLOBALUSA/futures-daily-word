export const meta = {
  name: 'daily-word-implement-fixes',
  description: 'Implement the audited Daily Word fixes in waves with exclusive file ownership, ending in a build gate',
  phases: [
    { title: 'Wave 1: backend + isolated', detail: '7 parallel agents, disjoint files' },
    { title: 'Wave 2: core screens', detail: 'HomeScreen + PlansScreen owners' },
    { title: 'Wave 3: UX flows', detail: 'HomeScreen UX + navigation/IA' },
    { title: 'Wave 4: accessibility', detail: 'a11y batch' },
    { title: 'Wave 5: i18n', detail: 'translation sweep' },
    { title: 'Gate', detail: 'build + tests + residual fixes' },
  ],
}

const PLAN = '/private/tmp/claude-501/-Users-ashleymarkevans/e38260d2-c95f-47c2-a933-b538b5604282/scratchpad/dw-plan.json'

const BASE = `
You are implementing verified audit fixes in the Futures Daily Word repo.

WORKTREE: /Users/ashleymarkevans/futures-daily-word-audit — branch fix/deep-dive-audit (from prod main). node_modules is installed. Do NOT commit, push, or branch. Do NOT touch ~/futures-daily-word (another session's WIP).

THE PLAN FILE: ${PLAN} — JSON with synthesis.fix_now[] and synthesis.ux_ship_now[] (each has rank, title, files, plan, source_ids) and confirmed[] (the underlying verified findings, keyed by id, with detail + proposed_fix + file/line evidence). Read YOUR items' full 'plan' text plus every confirmed[] entry whose id is in your items' source_ids before writing code.

RULES:
- OWNERSHIP IS ABSOLUTE: edit ONLY the files listed as yours. If your item's plan mentions a file you do not own, skip that part and note it in deferred_notes — another agent owns it.
- Verify before editing: findings were adversarially verified against this exact commit, but re-read each site; line numbers are from branch start and may have shifted if a prior wave touched the file — locate by code pattern. If the code genuinely contradicts a finding, skip that item and say why.
- Surgical edits in the repo's existing style. No drive-by refactors, no reformatting, no renames. Comments only for non-obvious constraints.
- LOAD-BEARING INVARIANTS (violating one = failure): applyCloudData must NOT include journal in jsonFields; applyMisc stays FILL-ONLY; deletes are tombstones, never hard; plan/reading/streak day logic uses LOCAL dates via toLocaleDateString('en-CA'); book plans (plan.bookId) stay EXCLUDED from the hero scripture pipeline; pushSupported() origin gating stays; keepalive fetch body stays capped; NO italics on body/scripture text; the hero reading panel stays light #FFFFFF in both themes; light theme stays default; no client-side Supabase; do not remove public/sw.js.
- i18n: t() returns the raw key when missing. If you add user-visible strings, either reuse existing keys or add the key to src/utils/i18n.ts for ALL FOUR languages (en/es/pt-BR/id) — never ship a key without all four.
- When done: run  npx tsc --noEmit -p tsconfig.app.json  in the worktree and fix any errors YOUR edits introduced (pre-existing errors in files you do not own: leave, but report).
- Final message = raw JSON only: {"done":[ids],"skipped":[{"id","why"}],"deferred_notes":[strings],"tsc":"clean|errors: <summary>"}`

const W1 = [
  { key: 'sync', prompt: `YOUR ITEMS: synthesis.fix_now rank 1 (stop sync wiping the only backup on pull failure) and rank 16 (sync resilience batch) — EXCEPT any JournalScreen.tsx parts (deferred, not yours).\nYOU OWN: src/utils/cloudSync.ts, netlify/functions/user-sync.js.\nExtra care: this is the data-safety core for 144 real users. The empty-guard on push (refusing to overwrite a non-empty stored journal/highlights/streak/plans with empty) must be server-side in user-sync.js and must not break the legitimate first-backup case for a genuinely new user.` },
  { key: 'security', prompt: `YOUR ITEMS: synthesis.fix_now rank 8 (lock down auth/PII endpoints), rank 9's BACKEND part only (scope analytics-dashboard.js access; the HomeScreen/CampusCountBadge parts are NOT yours), and rank 18 (cost/abuse hardening + dead-code purge + delivery edges) except any src/ or public/sw.js parts.\nYOU OWN: everything under netlify/functions/ EXCEPT user-sync.js and esv-audio.js (owned by others). That includes pco-sync.js, user-profile.js, lib/auth.js, lib/cors.js, polly-tts.js, bolls.js, analytics-dashboard.js, claude.js, elevenlabs-tts.js, biblebrain-audio.js, nlt.js, send-push2/, push-send.js, push-cron.js, export-profiles.js, pastor-admin.js.\nExtra care: do not break the app's real call patterns — check how src/ calls an endpoint before tightening it (read-only reads of src/ are fine; editing src/ is not). Removing dead code (nlt.js, send-push2 if truly unreferenced) requires grep-proof it is unreferenced from src/, netlify.toml and other functions.` },
  { key: 'highlights', prompt: `YOUR ITEM: synthesis.fix_now rank 2 (highlights mass-delete after cloud sync).\nYOU OWN: src/contexts/ScriptureSelectionContext.tsx.` },
  { key: 'streak', prompt: `YOUR ITEMS: synthesis.fix_now rank 3 (streak day-math to local dates) — the streak.ts core ONLY (the PlansScreen.tsx and HomeScreen.tsx call-site line fixes belong to other agents; note them as deferred) — and ux_ship_now rank 12 (streak memory and a graceful reset).\nYOU OWN: src/utils/streak.ts, src/components/DoneCelebration.tsx, src/utils/persona-config.ts, and src/utils/i18n.ts (APPEND new keys only — do not restructure).\nExtra care: the one-time upgrade tolerance (accept stored lastDate matching either the local or UTC rendering of today/yesterday) so no existing user's streak breaks on deploy.` },
  { key: 'audio', prompt: `YOUR ITEMS: synthesis.fix_now rank 4 (poisoned preload cache + ESV audio over the response cap) — EXCEPT the HomeScreen.tsx line-684 preload-slice change (deferred, not yours; implement the api.ts cache-key discriminator so colliding inputs cannot share a cache entry regardless) — plus rank 17's api.ts offline-placeholder part and ListenButton pause part, plus ux_ship_now rank 2 (Media Session + OS-pause sync) EXCEPT its HomeScreen part.\nYOU OWN: netlify/functions/esv-audio.js, src/utils/api.ts, src/utils/audioPlayer.ts, src/components/ListenButton.tsx.\nExtra care: the esv-audio 302-redirect approach must keep working through the futures.church proxy (media-src on the church CSP already allows audio.esv.org; verify the redirect target origin is audio.esv.org).` },
  { key: 'pwa', prompt: `YOUR ITEMS: synthesis.fix_now rank 12 (stop the SW registration wiping church-origin caches on each /daily-word visit), rank 14 (one manifest, right colors, right start_url; fix the 'Back to Futures Church' self-loop), and rank 10's public/sw.js part only.\nYOU OWN: public/sw.js, src/main.tsx, index.html, manifest.json (repo root), public/manifest.json, vite.config.ts, src/components/Seam.tsx.\nContext for the Seam fix: futures.church/daily-word-app (an iframe embed of this app) is now LIVE — the app's 'Futures Church ↗' seam link should take users to https://futures.church (the church site), not back into a Daily Word surface. Keep VITE_CHURCH_DAILY_WORD_URL overridability.\nExtra care: do NOT remove or blank public/sw.js and do not change the app-origin SW's content-hash update model; the fix is about what runs when the app is served on the CHURCH origin (embed/proxy) — there it must not register a SW or clear caches that belong to the church.` },
  { key: 'components', prompt: `YOUR ITEMS: synthesis.fix_now rank 15 (dead/broken visible controls batch) EXCEPT its PlansScreen.tsx part (deferred); rank 16's and 15's JournalScreen.tsx parts; rank 17's comfort UTC rotation (ComfortSection/ComfortCard); and ux_ship_now rank 11 (keep the promises to vulnerable users) EXCEPT its HomeScreen part.\nYOU OWN: src/components/NewBelieverLessonCard.tsx, src/components/ComfortSection.tsx, src/components/ComfortCard.tsx, src/components/PastoralReflectionSection.tsx, src/screens/JournalScreen.tsx, plus any other small src/components file rank 15 names that no other agent owns (check the plan file; HomeScreen/PlansScreen/MoreScreen/MessagesScreen are NOT yours).\nIf you add user-visible strings, add i18n keys for all four languages.` },
]

const W2 = [
  { key: 'homescreen', prompt: `YOU ARE THE SOLE OWNER OF src/screens/HomeScreen.tsx (~3,700 lines) plus src/components/CampusCountBadge.tsx for this wave.\nYOUR ITEMS (implement each one's HomeScreen/CampusCountBadge parts; other files mentioned are done by others): fix_now rank 3 (line ~554 local-date fix), rank 4 (line ~684: preload must pass the same text slice as playback), rank 5 (persist dw_hero_chapter_idx keyed by heroKey), rank 6 (hero play button loading/error/Restart), rank 7 (plan-completion integrity + finish celebration + book-plan progress display), rank 9 (replace fabricated pastor-facing campus numbers with real analytics data — read the plan for exactly what replaces what), rank 10 (lines ~896-899 book path part), rank 11 (calcPlanDay date-only startedAt parse, lines ~111-122 and ~839), rank 17 (lines ~692-703 and ~754-786), and ux_ship_now rank 1 (listening to the end of the audio chain marks the day read + triggers the same celebration as tapping Mark as read).\nWave-1 agents already edited api.ts/audioPlayer/streak.ts — your call-sites must match their new signatures if changed (read those files first).\nExtra care: markPlanDayComplete only on result==='ended'; the book-plan exclusion invariant (bookId plans never feed the hero fetch pipeline) must survive every change.` },
  { key: 'plansscreen', prompt: `YOU ARE THE SOLE OWNER OF src/screens/PlansScreen.tsx for this wave.\nYOUR ITEMS (their PlansScreen parts only): fix_now rank 3 (streakDisplay local dates, lines ~96-98), rank 10 (book plans broken for es/pt/id: double-suffixed paths + SPA-HTML cached as JSON, lines ~62-71, 152, 178 — coordinate with the already-applied public/sw.js fix by reading it), rank 11 (calcPlanDay duplicate, lines ~106-117), rank 15 (line ~563 dead control).\nAlso fix the copy/behavior mismatch: the screen says "Tap a plan to start it" but tapping only SELECTS and reveals a separate "Start This Plan" button — align the copy with the actual two-step behavior (update all four languages if the string is keyed).` },
]

const W3 = [
  { key: 'home-ux', prompt: `YOU ARE THE SOLE OWNER OF src/screens/HomeScreen.tsx, src/components/InlineReflection.tsx, src/components/PastorStudyOnboarding.tsx for this wave. HomeScreen was heavily edited in the prior wave — line numbers in the plan file are STALE for it; locate every site by code pattern.\nYOUR ITEMS (their parts in your files): ux_ship_now rank 3 (chaptersPerDay default 3 → 1 — the HomeScreen default only; MoreScreen/App parts are another agent's), rank 5 (comfort and new-believer users land on their content, not a plan-setup funnel), rank 6 (state-aware plan CTAs; stop bypassing the pastor wizard), rank 11's HomeScreen part (savePathway completion moment), rank 13 (unify the two day navigators; stop the lower strip killing audio), rank 14 (move commentary next to the passage; cover every passage of the day), rank 16's HomeScreen + InlineReflection parts (search surfacing, Stop keeps your place, journal hand-off).\nAlso: for a brand-new user with no reading history, the header streak chip must not show "1 day / Welcome back." — show nothing (or a first-visit-appropriate greeting) until a real streak exists; check src/utils/streak.ts's new API for what a fresh state looks like.\nExtra care: the owner-locked hero design (photo plate as one big play button, light panel) stays; you are re-ordering/gating content and fixing behavior, not restyling.` },
  { key: 'nav', prompt: `YOU ARE THE SOLE OWNER of src/App.tsx, src/screens/MoreScreen.tsx, src/screens/MessagesScreen.tsx, src/screens/LibraryScreen.tsx, src/screens/JournalScreen.tsx, src/components/BibleAI.tsx, src/components/PushOptIn.tsx, src/data/sermons.ts, and any NEW files you create (e.g. src/components/CampusSelect.tsx, src/utils/useSubView.ts).\nYOUR ITEMS: ux_ship_now rank 4 (browser-history for sub-views so the back-gesture stops ejecting users — implement for App-level tab state and YOUR screens; PlansScreen/HomeScreen adoption is deferred, note it), rank 3's MoreScreen/App parts (chaptersPerDay default + per-persona seed at pathway pick), rank 8 (sermon notes: stop the three stores lying to each other — step 1, additive only), rank 10 (campus tab empty state gets an actual campus picker), rank 15 (pastor admin friction: remember the code, retire window.prompt, group Admin, fix the false data notice), rank 16's App.tsx part (tab re-tap returns to tab root), rank 17 (bridge church-proxy users to the native origin with the same-email safety rail).\nExtra care: history integration must not break the existing Android-back kill-switch behavior if one exists (read App.tsx first); popstate handling must be idempotent. New user-visible strings need all four i18n languages.` },
]

const W4 = { key: 'a11y', prompt: `YOU OWN every file you touch this wave (prior waves are finished; still do not touch files outside the app's src/ + index.html).\nYOUR ITEM: ux_ship_now rank 9 — the full accessibility batch: real dialog semantics + focus trap + Escape for modals (EmailGate, PushOptIn, PathwayPicker, VerseNoteDrawer, GreekHebrewPopup, SetupPromptModal and any other overlay), accessible names for icon-only controls (play/pause, day arrows, theme toggle, tab bar, the five persona buttons on the welcome screen — these are currently UNNAMED buttons), keyboard-reachable verse selection/HighlightToolbar, aria-live for toasts/copy confirmations, the hero chapter slider's name+value, minimum hit areas where the plan file cites them, and the Space-key hijack fix (confirmed finding A11Y-2).\nAlso the confirmed I18N-5 finding: <html lang> must update on in-place language switch.\nUse the confirmed[] entries with ids starting A11Y- and I18N-5 in the plan file as your spec. Keep DOM structure changes minimal — semantics via attributes and small wrappers, no visual redesign (the look is owner-locked).` }

const W5 = { key: 'i18n', prompt: `YOU OWN src/utils/i18n.ts and every src/ file needing string keying (prior waves are finished).\nYOUR ITEM: ux_ship_now rank 7 — the i18n sweep: first wire the ~75 already-translated-but-unused keys the audit found (confirmed I18N-2 lists the mapping evidence), then key the remaining hardcoded user-facing English in the celebration/comfort/wizard layer with new keys translated to ALL FOUR languages (en/es/pt-BR/id). Respect the file's existing \\uXXXX escape style. Also fix confirmed finding I18N-1 (raw keys rendering on the new-believer lesson card: 'day_label 3 of_label 30') and I18N-3 (state-blind 'Toggle audio' aria-labels) if wave 4 did not already.\nPriority order if the sweep is large: (1) anything currently rendering RAW KEYS on screen, (2) first-run/onboarding surfaces, (3) comfort + new-believer surfaces, (4) the rest. Translations must be real translations in the app's existing register (check neighbouring keys for tone), not machine-literal placeholders.` }

const RESULT_SCHEMA = { type: 'object', required: ['done', 'skipped', 'deferred_notes', 'tsc'], additionalProperties: false, properties: {
  done: { type: 'array', items: { type: 'string' } },
  skipped: { type: 'array', items: { type: 'object', required: ['id', 'why'], additionalProperties: false, properties: { id: { type: 'string' }, why: { type: 'string' } } } },
  deferred_notes: { type: 'array', items: { type: 'string' } },
  tsc: { type: 'string' } } }

const results = {}
log('Wave 1: 7 parallel agents on disjoint backend/component files')
const w1 = await parallel(W1.map(d => () => agent(`${d.prompt}\n${BASE}`, { label: `w1:${d.key}`, phase: 'Wave 1: backend + isolated', schema: RESULT_SCHEMA }).then(r => ({ key: d.key, ...r }))))
results.wave1 = w1.filter(Boolean)

log('Wave 2: HomeScreen + PlansScreen owners')
const w2 = await parallel(W2.map(d => () => agent(`${d.prompt}\n${BASE}`, { label: `w2:${d.key}`, phase: 'Wave 2: core screens', schema: RESULT_SCHEMA }).then(r => ({ key: d.key, ...r }))))
results.wave2 = w2.filter(Boolean)

log('Wave 3: UX flow agents')
const w3 = await parallel(W3.map(d => () => agent(`${d.prompt}\n${BASE}`, { label: `w3:${d.key}`, phase: 'Wave 3: UX flows', schema: RESULT_SCHEMA }).then(r => ({ key: d.key, ...r }))))
results.wave3 = w3.filter(Boolean)

log('Wave 4: accessibility batch')
results.wave4 = [await agent(`${W4.prompt}\n${BASE}`, { label: 'w4:a11y', phase: 'Wave 4: accessibility', schema: RESULT_SCHEMA })]

log('Wave 5: i18n sweep')
results.wave5 = [await agent(`${W5.prompt}\n${BASE}`, { label: 'w5:i18n', phase: 'Wave 5: i18n', schema: RESULT_SCHEMA })]

log('Gate: full build + tests')
const gate = await agent(`You are the build gate for the Daily Word fix branch.\nWORKTREE: /Users/ashleymarkevans/futures-daily-word-audit (branch fix/deep-dive-audit). Do NOT commit or push.\nRun in order and fix what breaks, minimally and in-style: (1) npx tsc --noEmit -p tsconfig.app.json (2) npm run build (3) npx vitest run (4) npx eslint on CHANGED files only (git diff --name-only) — fix errors your judgment says are real regressions from this branch's edits; pre-existing lint debt stays.\nThen sanity-scan git diff for: any violation of these invariants — applyCloudData excludes journal; applyMisc fill-only; tombstone deletes; local-date day math; book-plan hero exclusion; sw.js kill-switch intact on the church side (not in this repo); no italics on scripture/body; light default theme; hero panel #FFFFFF both themes; every new i18n key present in all four languages (write a quick script to diff key sets) — and fix violations you find.\nFinal message = raw JSON: {"build":"pass|fail: why","tests":"pass|fail: summary","fixed":[strings],"invariant_flags":[strings],"files_changed":<number>}`, { label: 'gate:build', phase: 'Gate', effort: 'high', schema: { type: 'object', required: ['build', 'tests', 'fixed', 'invariant_flags', 'files_changed'], additionalProperties: false, properties: { build: { type: 'string' }, tests: { type: 'string' }, fixed: { type: 'array', items: { type: 'string' } }, invariant_flags: { type: 'array', items: { type: 'string' } }, files_changed: { type: 'number' } } } })

return { results, gate }